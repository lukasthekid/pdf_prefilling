fullName: "{{ first_name }}, {{ last_name }}"
dateOfBirth: "{{ birthdate }}"
companyCode: "{{ company_code }}"
address: "{{ street }}, {{ house_number }}"
postCode: "{{ city }}, {{ post_code }}"
maritalStatus: "{{ family_status }}"
nationality: "{{ nationality }}"
phoneNumber: "{{ phone_number }}"
email: "{{ email }}"
